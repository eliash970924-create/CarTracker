*.iml
.gradle
/local.properties

# Android Studio / IntelliJ project state. Machine-specific (JDK paths,
# emulators, last deploy target) and rewritten on almost every IDE action,
# so it churns the working tree without ever being useful in history.
# The IDE regenerates it on open.
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
